# 2026-04-29

## What I Learned
- Discord JSON delivery via shell `curl` fails silently with 403 — Python `urllib` POST is more reliable
- i-En monitoring: zero notifications when healthy, only alert on problems (3 checkpoints: token/VERSION/FATAL)
- Hermes delivery pattern: claims done but only posts Discord code without writing to disk — needs tag confirmation

## Notes

### GitHub Trending Channel Setup
- Future posts → #github-trending channel: `1492218923406594199`
- Cron job `e2a069c0` delivery target confirmed correct

### OpenClaw Updates Channel Setup
- Future posts → #openclaw-updates channel: `1494120970703863948`
- Cron job `85a00b06` (OpenClaw Release Tracker) running every 6 hours

### Bug Fix: Discord JSON Delivery (Critical)

**Problem**: Cron job used shell `curl` + `python3 -c json.dumps()` to拼接 Discord JSON → API returned 403 (Invalid Form Body). curl failed silently in background with no error logs.

**Root cause**: Shell JSON concatenation mishandles special characters

**Fix**: Rewrote `check_release.py` with `post_to_discord()` using Python `urllib` directly
- Backfilled `2026.4.26` (2026-04-28) to #openclaw-updates (Message ID: `1498902296275451907`)
- Cron payload simplified to `python3 check_release.py` (fetch + auto-post in one)

### i-En Lightweight API Monitoring Script

**Design principle**: Simple but thorough — zero notifications when healthy, only react when problems occur

**3 checkpoints:**
1. **Token status** — GET `/me?fields=id` to test validity
2. **API version** — check against supported list
3. **FATAL log** — read last 24 hours of FATAL records

**Files created/modified:**
| File | Purpose |
|------|---------|
| `src/config_backup.js` | Token reading with Backup fallback |
| `src/health_check.js` | Token validity / API version / FATAL log check |
| `monitor.js` | Integrated healthCheck(), skips duplicate notifications when healthy |

**Verification (0429):**
- Token valid ✅ (200 OK)
- API v21.0 supported ✅ (not deprecated)
- FATAL log: 4 entries (yesterday 16:02-16:18 GMT+8), healthy=false, monitor.js correctly skipped duplicate notification ✅
- Zero notification principle confirmed ✅

**SPEC document**: `.../project_ien/SPEC.md`

### Hermes Delivery Pattern Issue

**Observation**: Hermes claimed completion but only posted Discord code — no file written to disk

**Handling**: brtclaw created the file directly and documented this pattern

**Tag rule reinforced**: All channel delivery tasks must tag Hermes with `<@1495572350270443721>` at message start, no longer relying on sessions_send

### Tomorrow's Tasks
- [ ] Observe i-En pipeline for new FATAL logs
- [ ] Track 190 Token expiration and replacement process

## Tags
#github-trending #openclaw-updates #discord-json #i-en-monitoring #hermes-delivery-issue

## Resources
- GitHub Trending channel ID: `1492218923406594199`
- OpenClaw Updates channel ID: `1494120970703863948`
- Hermes Discord UID: `1495572350270443721`